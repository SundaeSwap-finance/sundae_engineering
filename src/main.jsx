import React from "react";
import ReactDOM from "react-dom/client";
import SundaeSite from "./SundaeSite";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SundaeSite />
  </React.StrictMode>
);
